pipeline {
  agent {
    node {
      label 'win-agent'
    }

  }
  stages {
    stage('clean') {
      steps {
        bat 'dsk'
      }
    }

    stage('compile') {
      steps {
        bat 'set'
      }
    }

  }
}